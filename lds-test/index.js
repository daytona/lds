module.exports = function test(type, config) {
  console.log('Test');
}

/**
 * Tests to expect
 * : is process.cwd() a lds directory
 * : lds-config.js exists and needed properties
 * : Non empty default.json
 * : ...
 */
